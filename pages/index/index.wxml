<!--index.wxml-->
<view class="container">
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
  <view>
    <button class="recommendbtn" bindtap='recommenddish'>开始推荐</button>
    <!-- <button class="weui-btn mini-btn" type="default" size="mini" bindtap='alldish'>所有菜式</button> -->
  </view>
  <view class="weui-footer weui-footer_fixed-bottom">
            <!-- <view class="weui-footer__links">
                <navigator url="" class="weui-footer__link">WeUI首页</navigator>
            </view> -->
            <view class="weui-footer__text">Copyright © 2017-2018 yammi</view>
        </view>
</view>
